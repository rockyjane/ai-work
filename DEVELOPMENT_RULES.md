# DEVELOPMENT_RULES — Claude Code 開發行為規範

> 這份檔案是給 Claude Code（含任何 subagent）閱讀並遵守的行為底線。
> 本檔已由 `CLAUDE.md` 以 `@` 自動載入，每個 session 開工前即生效。以下規則優先於一切臨場判斷,任何一條都不得自行略過。
> 若某項操作本檔未涵蓋但你判斷有風險,一律停下來詢問使用者,不要自行決定。

---

## 一、Git 操作規範

### 允許自行執行
- `git add`、`git commit`、`git pull`、`git push`、`git checkout`、`git fetch`、`git status`、`git log`、`git diff`、開新分支。

### 必須先詢問、取得明確同意才可執行
- **分支合併 `git merge`**：不得自行合併。判斷某分支需合併時,停下來回報三件事——為什麼要合併、合併後會帶來什麼改變、改動了哪些檔案——經使用者同意後才進行。

### 絕對禁止（違反會造成不可逆的工作損失）
- `git reset --hard`（丟棄未提交變更）
- `git push --force` / `git push -f`（覆蓋遠端歷史）
- `git clean -f`（刪除未追蹤檔案）
- `git checkout .` / `git restore .` 等會一次丟棄大量未提交變更的操作
- 任何會清除或覆蓋「尚未提交」工作的指令
→ 若你認為真的需要類似操作,一律先停下說明原因並詢問,不得自行執行。

### 分支策略（重要）

分支階層——功能分支從「主線」開、合回「主線」，只有主線才進 `main`：

```
main                  穩定線；只收 front-end / back-end 的合併（且須先問，見上）
├── front-end         前端主線（自 main 開）
│   └── feature/xxx    前端功能分支（自 front-end 開 → 完成後合回 front-end）
└── back-end          後端主線（自 main 開）
    └── feature/yyy    後端功能分支（自 back-end 開 → 完成後合回 back-end）
```

- **每次要開發新功能/任務前，先開一條新分支**，且**必須從對應主線（`front-end` 或 `back-end`）開**——不要直接從 `main` 開、也不要把所有分支都合到 `main`（那樣 main 會又雜又危險）。這樣每條分支歸屬清楚、好追是誰做了什麼。
- **功能分支完成 → 合回它所屬的主線**（前端功能合回 `front-end`、後端合回 `back-end`），不直接合進 `main`。
- **只有 `front-end` / `back-end` 主線才合併進 `main`**，且合併一律先問（見「必須先詢問、取得明確同意」）。
- ⚠️ **切分支／開新分支前，先把手上未提交的改動收好**——至少 `git add` stage 起來，最好 `git commit` 或 `git stash`——**不可放著未提交的改動就切/開分支**。遇到急件、插隊的新需求、或 PR 留言很急要改碼時**尤其如此**：先 `git status` 確認、把現場 stage/stash 好，再切走去處理。

---

## 二、檔案與系統操作規範

- **刪除檔案前**（rm、刪除資料夾等）：一律先列出要刪什麼、為什麼,詢問後才刪。
- **改動設定檔前**（package.json、設定檔、CI 設定、建置設定等）：先說明要改什麼、影響是什麼,再動手。
- **絕不碰金鑰相關檔案**：.env、含 API 金鑰/密碼/token 的檔案,不讀取內容外傳、不覆蓋、不刪除;需要時只提醒使用者自行處理。
- **不擅自安裝套件**：需要新增依賴（npm install 等）時,先說明為什麼需要這個套件、它做什麼,經同意再裝。
- **不把真實/敏感資料送到外部正式服務或正式資料庫做測試**:測試一律用假資料或測試環境,避免洩漏真實資訊或污染正式資料。（讀取公開資料的唯讀 API——如抓台股行情的 FinMind——不在此限。）

---

## 三、開發流程規範

- **開工前先講計畫**:動手前用一段話說明打算改哪些檔、為什麼、預期結果。需求不清楚時先列問題,不要自行假設就大改。
- **任務拆到最小**:一次只專注完成一個最小邏輯模組,不要一口氣生成龐大複雜的整份程式碼。優先保持模組彈性與可重用。
- **最小變動原則**:只改與當前任務相關的部分,不順手大改無關檔案。
- **完工後自我檢查**:完成後自行檢視程式碼,若環境允許則執行相關測試,然後回報:改了哪些檔、做了什麼、測試結果、有沒有不確定需使用者確認的地方。
- **產出視為草稿**:你生成的程式碼一律當成待審查的草稿,提醒使用者檢查後再使用,不預設可直接上正式環境。

---

## 四、程式碼品質與安全

- 生成的程式碼要留意常見安全問題(輸入未驗證、注入風險、把密鑰寫死在程式裡等),發現時主動指出。
- 不把任何真實的帳密、金鑰、個資寫進程式碼或範例中,一律用佔位字串(如 YOUR_API_KEY)。
- 若使用到外部來源的程式片段,提醒可能的授權(license)問題,不確定授權的程式碼先告知使用者。

---

## 五、記錄（依實際分流，詳見 CLAUDE.md）

完成有意義的**里程碑**、或遇到值得記的雷/觀念/決策時,主動寫入對應檔（寫完提醒使用者是否提交）:

- **專案開發**（程式、設計、學到的觀念/知識）→ 寫**該專案的 `DEV_LOG.md`**:六大要點結構,每筆 `### 日期 時間 [寫入者] 分類`（分類:完成／雷／決策／觀念／實測）;並更新**根目錄 `DEV_LOG.md`** 該專案的一行摘要（根目錄只放跨專案簡要）。
- **環境／工具搭建**（Claude Code、gh、GitHub 授權推送、Python/Node/venv/Docker 等）→ 整合進 **`CLAUDE_CODE_SETUP_GUIDE.md`** 對應章節（說明書結構,不押時間戳）。
- 使用者**自行開發**的部分,不必自己手寫 DEV_LOG:交付/推送前由 Claude **依 diff 補寫**（寫入者標 `[me]`）;無法從 diff 推斷的雷/決策原因,先問使用者再寫,不自行臆測。

基準:以「有意義的里程碑」為單位（**不是每個 commit、也不是每次 merge**）,並**盡量把 DEV_LOG 條目跟該批程式放進同一個 commit**,讓時間戳對得上實際狀態。完整分流規則見 `CLAUDE.md`。

---

## 六、溝通

- 回覆一律使用繁體中文。
- 參照專案根目錄的 ABOUT_ME.md 與 CLAUDE.md,依其中的人設與技術熟練度決定語氣與註解詳細度。
- 遇到任何本檔未明確允許、又可能有風險的操作,預設行為是「停下來問」,不是「自己決定」。
