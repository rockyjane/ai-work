# CLAUDE.md

> 本檔案會在每個 Claude Code session 開始時自動載入，作為協助本專案開發的長期參考。

## 開發者背景

開發者的完整背景請見 @ABOUT_ME.md，協助開發時請一併納入考量。

## 溝通與回覆原則

- 一律使用**繁體中文**回覆。
- 介紹新的技術術語時，**先用白話解釋一次，再講細節**。
- 動工前先釐清規範與流程；產出的程式碼開發者會自行檢視與測試。
- 偏好**從最小模組開始搭建**、做高彈性／模組化以利重用，**不要一次產出整個專案**。

## 專案簡介

- 這是一個**股票分析網站**，作為重返金融業的面試 DEMO 兼學習用途。
- 技術組成：後端 FastAPI（Python）、前端 Vue 3 + Vite，位於 `stock-app/`。
- 開發者的股票領域知識仍在補強中，必要時請補充相關背景說明。

## 長期記錄要寫到哪一份？（請依性質分流）

記錄都跟著 Git 走、跨環境／跨 session 共用（不依賴會分散又可能被裁切的對話歷史）。遇到值得記的事，**主動寫入對應那份**，寫完提醒開發者，由其決定是否提交：

- **「專案開發」**的雷／好觀念／關鍵決策（程式碼、API、架構、套件、DB 等）→ 寫**該專案資料夾內的 `DEV_LOG.md`**（詳細版，保留「現象→原因→解法」，每筆 `## 日期 時間 [寫入者] 分類`）。**同時**到**根目錄 @DEV_LOG.md** 更新該專案那段的一行摘要——根目錄那份只放各專案「簡要摘要」，**不放細節**（共同＝簡要、專屬＝完整）。
- **「環境與工具搭建」**的雷／流程（Claude Code CLI／網頁版、`gh`、GitHub 授權與推送、Python／Node／venv／Docker 等）→ 寫 **@CLAUDE_CODE_SETUP_GUIDE.md**（說明書結構，**整合進對應章節或新增章節**，不是貼時間戳記）。

判斷原則：「**怎麼把環境/工具搭起來、授權、推送**」→ 搭建指南；「**這個專案怎麼寫、為何這樣設計**」→ 該專案的 DEV_LOG（＋根目錄摘要）。
